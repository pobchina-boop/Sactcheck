SACTCheck v0.68.0 deployment

Use the full project ZIP for the normal repository replacement workflow.

After replacement, install dependencies if required, then run the complete cumulative test suite and static site build before deployment.

This release includes intentional clinical source reconciliation in eleven high priority protocol records. Do not treat the release as independently clinically validated.
