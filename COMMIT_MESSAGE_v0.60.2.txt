fix(ui): move mission hero above the SACTCheck Engine

Corrects the v0.60.1 homepage ordering defect by moving the persistent mission hero to the top of the library screen and preventing the search-first controller from placing the regimen catalogue ahead of it.

Restores the intended flow of mission hero, Solid Tumour/Haematology selector, SACTCheck Engine search, collapsed clinical scenario interpreter and regimen cards. The search field remains the primary working entry point, while the interpreter remains secondary and marked as early development.

Removes the unintended full mission/promotional block from the end of the catalogue. The fuller product rationale, advantages and feasibility-evaluation explanation remain available as optional collapsed content inside the welcome modal, reducing duplication without losing information.

Preserves the welcome modal, Launch SACTCheck Engine behaviour, responsive styling, accessibility controls, fifteen-regimen knowledge base and library-wide organ-function reconciliation.

No protocol JSON, clinical threshold, dose action, organ-function rule, assessment-engine behaviour or knowledge-base profile is changed. All 382 protocol JSON files remain byte-for-byte unchanged from v0.60.1.
