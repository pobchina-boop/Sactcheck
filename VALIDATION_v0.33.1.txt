SACTCheck v0.33.1 validation

Scope
- Adds explicit patient-value versus protocol-cutoff comparison to every triggered generic JSON rule where the encoded condition contains an entered field and threshold.
- Retains the 58-protocol catalogue and all v0.33.0 rapid-assessment behaviour.

Display behaviour
- Patient result is displayed prominently on the left.
- The triggering comparison operator is displayed centrally.
- The exact encoded protocol cutoff is displayed prominently on the right.
- Units are inherited from the protocol input definition.
- Compound rules display each entered condition used by the triggered rule.
- Narrative explanation, action detail and NCCP source reference remain directly beneath the comparison.

Validation
- Rule findings now preserve the encoded triggering condition for transparent UI rendering.
- Dedicated v0.33.1 regression test added.
- Full cumulative automated test suite passes.
