Commit summary
fix(version): synchronize active SACTCheck release identity

Commit description
Synchronizes the cumulative SACTCheck release identity across the visible application while retaining separate internal implementation versions for individual modules.

Adds a central application release record and updates the main page title, header, footer and release metadata to v0.67.1.

Adds cumulative application cache keys to the regimen knowledge base, primary clinical validation workspace and NCCP Change Tracker so a new deployment loads the current assets instead of retaining an older browser cached module.

The NCCP Change Tracker keeps its historical module implementation version for traceability but now exposes the current application release separately.

Adds a focused regression test that fails if the public release identity or active module cache keys drift from the package release.

No clinical protocol rule, threshold, dose action, indication, organ function pathway, emetogenic classification or knowledge content is changed. All 382 clinical protocol JSON files remain unchanged from v0.67.0.
