SACTCheck v0.67.1

Use the full project ZIP for the normal repository replacement workflow.

This release synchronises the public application version and refreshes active module cache keys. It does not change clinical protocol content.

After deployment, refresh the application once. The public header should display v0.67.1. The knowledge base, validation workspace and NCCP Change Tracker should all load under the current application release even though their internal module implementation versions remain separately traceable.
