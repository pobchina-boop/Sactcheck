SACTCheck v0.75.1 — connected-support panel visibility hotfix

This is a cumulative drag-and-drop update over v0.75.0.

Apply:
1. Unzip this archive.
2. Drag the contents into the root of the SACTCheck repository.
3. Allow matching files to merge/replace.
4. Start/restart the local server.
5. Hard refresh once (Ctrl+Shift+R) if an already-open tab is showing cached CSS.

Hotfix:
- fixes the right-hand “One regimen, connected support” card where the step
  labels/subtext were present in the DOM but rendered white against a pale card.
- increases selector specificity so legacy v0.72 hero colours cannot override
  the v0.75 light-card text colours.
- retains all v0.75 patient-guide and supportive-care reconciliation changes.
