SACTCheck v0.76.0 — PDF-first patient guide + chemo-man visual language

This is a cumulative drag-and-drop update over v0.75.1.

Apply:
1. Unzip this archive.
2. Drag the contents into the root of the SACTCheck repository.
3. Allow matching files to merge/replace.
4. Start/restart the local server.
5. Hard refresh once (Ctrl+Shift+R) if an already-open tab is showing cached assets.

What this fixes:
- restores patient-facing output where the patient passport / patient guide had effectively disappeared
- patient-support actions now open the regimen-specific A4 / PDF guide directly
- patientSupport URL routing now also opens the printable guide automatically
- adds a simple chemo-man style body-map visual language so the page is not text only
- keeps the regimen-specific schedule and separates general, agent-specific and immunotherapy-related toxicities
