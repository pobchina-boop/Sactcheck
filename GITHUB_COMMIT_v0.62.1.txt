Commit summary

fix(tracker): present clinician facing NCCP source status

Commit description

Refines the NCCP Change Tracker so the visible application explains protocol currency, change review and reconciliation in clinician facing language.

Removes visible references to GitHub, repository workflows, pull requests, source fingerprints, extracted text processing, remote capture and other internal implementation details.

Simplifies the visible pathway to Detect, Compare, Review and Reconcile. Rewrites summary cards, source status messages, empty states, feature descriptions and the NCCP protocol register around the practical questions clinicians and reviewers need answered.

Renames Silent replacement to Unannounced change in the interface while preserving the underlying detection capability. Keeps separate Function and Strength explanations for all eight tracker features and retains the mandatory human review gate.

No clinical protocol JSON, treatment threshold, dose action, organ function rule, knowledge base profile or assessment engine behaviour is changed. All 382 protocol JSON files remain unchanged from v0.62.0.
