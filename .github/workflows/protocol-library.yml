name: Legacy protocol workflow (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    name: Legacy workflow retired
    runs-on: ubuntu-latest
    steps:
      - name: Explain replacement
        run: echo "This legacy write-capable workflow is retired. Use Validate SACTCheck and Deploy SACTCheck Pages."
