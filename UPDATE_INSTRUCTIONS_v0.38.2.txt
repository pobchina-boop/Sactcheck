SACTCheck v0.38.2 update

Replace the existing deployed Sactcheck folder with the complete folder in this ZIP, or copy all files while preserving the directory structure. Commit the full update so the v0.38.2 cache keys refresh the corrected tumour-site metadata and catalogue filtering logic.
