SACTCheck v0.73.1 — RELEASE LABEL / PATIENT-DIRECTION HOTFIX

WHY THIS IS NEEDED
The v0.72 regimen workflow engine reasserts its own app title, header version and older
Assess / Consent / Support / Inform homepage language after the patient-content module loads.
That is why v0.72 can remain visible even after the v0.73 files were correctly copied.

WHAT THIS HOTFIX DOES
- Leaves the v0.72 workflow/assessment engine logic untouched.
- Makes the patient-content module reassert the cumulative v0.73 presentation after workflow refreshes.
- Keeps the visible header version at v0.73.0.
- Keeps the browser title at SACTCheck v0.73.0 - Patient Content Pipeline.
- Reasserts the patient-first homepage wording after protocol/workflow events.
- Does not change protocol thresholds, dose rules, NCCP encodings or assessments.

HOW TO APPLY
1. Extract this ZIP.
2. Drag the js folder into the ROOT of C:\Users\Paul\Documents\GitHub\Sactcheck
3. Merge folders and REPLACE js/regimen-consent-builder-v0710.js.
4. Hard refresh with Ctrl+F5.
5. You should now see v0.73.0 in the header.

CHECK
- Header shows v0.73.0.
- Regimen cards show Patient support and no + Agent.
- Patient support opens.
- Existing assessment/workflow features still work.
