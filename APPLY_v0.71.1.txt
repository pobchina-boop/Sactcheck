SACTCheck v0.71.1 - STREAMLINED TWO-PAGE CONSENT PDF
OLD-STYLE DRAG / DROP / REPLACE

1. Extract this ZIP normally.
2. Open the extracted folder.
3. Ctrl+A -> Copy.
4. Paste everything into:
   C:\Users\Paul\Documents\GitHub\Sactcheck
5. Choose "Replace the files in the destination".
6. Open Command Prompt / VS Code terminal in the Sactcheck folder.
7. Run:
   npm.cmd test
8. DO NOT commit or push if any test fails.

If green, hard-refresh the local site and smoke-test:

A. Standard regimen
   - Find FOLFOX (or another chemotherapy regimen).
   - Click "Consent PDF".
   - Confirm a PDF downloads immediately.
   - Confirm the PDF is exactly 2 pages.

B. Add agent
   - Click "+ Agent".
   - Type "Avastin".
   - Select Bevacizumab.
   - Confirm it appears as a "clinician added" chip.
   - Click "Generate consent PDF" or close the popup and click "Consent PDF".
   - Confirm Bevacizumab appears in the regimen/agent-specific section and is labelled CLINICIAN ADDED.

C. Immunotherapy addition
   - Click "+ Agent".
   - Type "Keytruda".
   - Select Pembrolizumab.
   - Generate the PDF.
   - Confirm the immunotherapy section appears automatically on page 2.

D. Remove
   - Re-open "+ Agent".
   - Remove an added agent.
   - Confirm the next PDF no longer contains it.

This package does NOT modify protocol JSON or deterministic treatment logic.
