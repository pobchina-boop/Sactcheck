SACTCheck v0.65.0

For the normal cumulative repository workflow, use the full project ZIP.

The drop in ZIP contains the files changed by this knowledge base release, the Avelumab source metadata reconciliation, tests and release documentation.

After applying the update, run npm test, npm run security:check, npm run build:pages and npm run security:site.
