Commit summary

fix: harden source fidelity and audit controls

Commit description

Completes the deepest whole system SACTCheck audit and remediation to date.

Rebuilds or materially reconciles eleven high priority breast protocol records against their current mapped NCCP sources, including corrected blood count boundaries, DPD pathways where applicable, renal and hepatic rules, cardiac context where applicable, toxicity actions and recurrent dose level logic.

Adds mandatory encoding maturity across all active protocols, explicit input roles across the complete input library, source objects and explanations across all executable rules, and canonical protocol identifiers while retaining legacy identifier compatibility.

Removes unreachable select conditions, resolves same component exact condition action conflicts, corrects the modified FOLFIRINOX hand foot syndrome rule and standardises tiered renal decisions as protocol specific selectors.

Corrects tissue mapping after direct source confirmation. NCCP 00212 is not a Lung protocol in the current source. NCCP 00215 remains cross listed to Breast, Gynaecology, Gastrointestinal and Lung.

Hardens search rendering against raw query HTML interpolation, strengthens validation export privacy confirmation, standardises visible Encoded rule coverage wording and adds cumulative runtime cache keys.

Normalises the active knowledge evidence taxonomy, adds canonical evidence identifiers and complete structured trial fields, and excludes historical knowledge data from the public static site build.

Adds a comprehensive audit disposition register, source fidelity register, clinical class screening queue and dedicated v0.68.0 regression coverage.

No active protocol is marked as clinically authorised. Formal consultant oncology and oncology pharmacy validation remains pending.
